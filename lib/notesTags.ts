import { Tag } from "@prisma/client";

export const TAG_OPTIONS:string[] = Object.values(Tag);


const query ='age 25, weight 61 kg, height 5.7 on statin 20 mg give me pakistani calorie surplus diet'


/* 

- on Creating new note: it should add it up on that arry (having all notes fetched).
- on Updating/editing note it should replace that note with the updated one in that array.
- on Deleting/removing note it should filter/remove that note from that array.


*/
